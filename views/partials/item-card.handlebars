<style>
        .trunctext {
                overflow: hidden;
                text-overflow: ellipsis;
                display: -webkit-box;
                -webkit-line-clamp: 2;
                line-clamp: 3;
                -webkit-box-orient: vertical;
        }
</style>

<div class="flex justify-center w-full m-3 cursor-pointer h-96 md:w-1/2 lg:w-1/3 xl:w-1/4 2xl:w-1/5"
        data-bs-toggle="modal" data-bs-target="#image{{id}}">
        <div
                class="p-3 transition duration-500 transform bg-white border border-gray-200 rounded-lg drop-shadow-lg hover:scale-105 hover:drop-shadow-xl">
                <div class="grid place-items-center">
                        {{#if images}}
                        {{#each images as |image|}}
                        <img class="object-cover w-full rounded-t-lg h-52" src="{{link}}" alt="{{description}}" />
                        {{/each}}
                        {{/if}}
                </div>
                <div class="p-1">
                        {{!-- <a href="#"> --}}
                                <h5 class="mb-2 text-xl font-bold tracking-tight text-gray-800">{{name}}</h5>
                                <p class="mb-3 font-normal text-gray-600 trunctext">{{description}}</p>
                                {{!-- <a href="#"
                                        class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-blue-700 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
                                        Edit
                                        <svg class="w-4 h-4 ml-2 -mr-1" fill="currentColor" viewBox="0 0 20 20"
                                                xmlns="http://www.w3.org/2000/svg">
                                                <path fill-rule="evenodd"
                                                        d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z"
                                                        clip-rule="evenodd"></path>
                                        </svg>
                                </a> --}}
                </div>
        </div>
</div>


{{!-- Modal for looking at an image full-size --}}
{{> image-modal}}
