<a href="/collections/{{id}}">
    <div class="p-2 transition duration-500 transform hover:scale-105">
        <div class="p-6 bg-white rounded-lg shadow-lg">
            <h2 class="mb-2 text-2xl font-bold text-gray-800">{{name}}</h2>
            <p class="text-gray-700">{{description}}</p>
        </div>
    </div>
</a>